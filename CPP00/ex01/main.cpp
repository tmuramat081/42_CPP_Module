#include <iostream>

int main() {
    std::string line;

    std::cout << ">> TINY PHONE BOOK APP << " << std::endl;
    while (true) {
        std::cin >> line;
        if (line == "ADD") {
            
        }
        if (line == "SERCH") {

        }
        if (line == "EXIT") {
            return (0);
        }  
    }

    return 0;
}